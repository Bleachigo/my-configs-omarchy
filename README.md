# ⚙️ Ultimate Dotfiles Guide (GNU Stow)

Цей репозиторій призначений для централізованого зберігання конфігурацій. Завдяки [GNU Stow](https://www.gnu.org), конфіги зберігаються в одній папці та автоматично створювати симлінки в домашній директорії.

---

## 🅰️ Етап А: Підготовка середовища

Перед початком переконайтеся, що GNU Stow встановлено:

- **Arch:** `sudo pacman -S stow`
- **Ubuntu/Debian:** `sudo apt install stow`
- **macOS:** `brew install stow`

---

## 🅱️ Етап Б: Перше створення репозиторію

1. Створіть папку в домашній директорії:
   ```bash
   mkdir ~/dotfiles && cd ~/dotfiles
   git init
   Будьте обачні, використовуючи код.
   ```

Створіть файл .gitignore, щоб не пушити сміття:
bash
touch .gitignore
echo "\*.log" >> .gitignore
echo ".DS_Store" >> .gitignore
Будьте обачні, використовуючи код.

📂 Етап В: Міграція існуючих конфігів (на прикладі nvim)
Цей процес потрібно повторити для кожного додатка:
Створіть імітацію структури: (Важливо: шлях всередині має повторювати шлях від ~)
bash
mkdir -p ~/dotfiles/nvim/.config/nvim
Будьте обачні, використовуючи код.

Перемістіть файли:
bash

# Спочатку копіюємо для перевірки

cp -r ~/.config/nvim/\* ~/dotfiles/nvim/.config/nvim/

# Створюємо тимчасовий бекап оригіналу (Stow не працює, якщо місце зайняте)

mv ~/.config/nvim ~/.config/nvim.bak
Будьте обачні, використовуючи код.

Активація через Stow:
bash
cd ~/dotfiles
stow -v nvim
Будьте обачні, використовуючи код.

Перевірка:
bash
ls -la ~/.config/nvim

# Ви маєте побачити: nvim -> ../../dotfiles/nvim/.config/nvim

Будьте обачні, використовуючи код.

☁️ Етап Г: Пуш на GitHub
Створіть порожній репозиторій на GitHub.
Запуште локальні файли:
bash
git add .
git commit -m "feat: initial structure with nvim config"
git branch -M main
git remote add origin https://github.com
git push -u origin main
Будьте обачні, використовуючи код.

🔄 Етап Д: Розгортання на НОВОМУ комп'ютері (Deployment)
Коли ви пересіли за нову машину, процедура виглядає так:
Встановіть Stow (див. Етап А).
Клонуйте репозиторій:
bash
git clone https://github.com ~/dotfiles
cd ~/dotfiles
Будьте обачні, використовуючи код.

Важливо: Якщо система вже створила стандартні конфіги (наприклад, пустий .bashrc), видаліть їх, інакше Stow видасть конфлікт:
bash
rm ~/.bashrc
Будьте обачні, використовуючи код.

Запустіть Stow для всіх папок відразу:
bash
stow \*/
Будьте обачні, використовуючи код.

🛠 Довідник команд та вирішення проблем
Ситуація Команда
Додали новий файл у ~/dotfiles/nvim/ stow -R nvim (Restow оновлює посилання)
Хочете видалити конфіг із системи stow -D nvim (Видаляє тільки посилання)
Помилка _has items not owned by stow_ Означає, що в ~/ вже є файл з таким іменем. Видаліть його або перейменуйте.
⚠️ Попередження
Ніколи не запускайте stow з папки, яка не є коренем вашого репозиторію.
Ніколи не зберігайте в цьому репозиторії приватні SSH-ключі або API-токени без шифрування (наприклад, git-crypt).
